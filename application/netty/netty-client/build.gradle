plugins {
    id "com.github.johnrengelman.shadow" version "5.1.0"
}

jar {
    manifest {
        attributes 'Main-Class': 'org.hopto.delow.chat.Client'
    }
}

dependencies {
    implementation (
            project(':domain')
    )
    implementation 'io.netty:netty-all:4.1.38.Final'
}
